# 🔨 Инструкция по пересборке .exe для релиза v1.1.0

## Шаги для пересборки

### 1. Убедитесь что все зависимости установлены
```bash
pip install -r requirements.txt
pip install pyinstaller
```

### 2. Закройте SpeechManager.exe если запущен
- Проверьте системный трей
- Проверьте диспетчер задач

### 3. Очистите старые файлы сборки (опционально)
```bash
# Вручную удалите папки:
rmdir /s /q build
rmdir /s /q dist
```

### 4. Запустите сборку
```bash
build.bat
```

Или вручную:
```bash
pyinstaller --clean build_exe.spec
```

### 5. Проверьте результат
После сборки должен появиться файл:
```
dist\SpeechManager.exe
```

### 6. Упакуйте релиз
```bash
pack_release.bat
```

Это создаст архивы в папке `releases\`:
- `SpeechManager-v1.1.0-win64.zip` - готовый .exe для пользователей
- `SpeechManager-v1.1.0-source.zip` - исходный код

### 7. Загрузите архивы на GitHub Release

Перейдите на:
https://github.com/haqury/speech_manager/releases/tag/v1.1.0

И загрузите созданные архивы в раздел "Assets".

## Что изменилось в v1.1.0

- ✅ Добавлен модуль `i18n.py` (уже включен в `build_exe.spec`)
- ✅ Добавлена локализация статусных сообщений
- ✅ Автоматическое определение языка по раскладке клавиатуры

## Проверка после сборки

После сборки проверьте:
1. ✅ `dist\SpeechManager.exe` существует
2. ✅ Размер файла ~80-120 MB
3. ✅ Запускается без ошибок
4. ✅ Статусные сообщения отображаются на правильном языке

## Если возникли проблемы

1. **Ошибка импорта i18n:**
   - Убедитесь что `i18n.py` в корне проекта
   - Проверьте что он добавлен в `build_exe.spec` в `hiddenimports`

2. **Большой размер .exe:**
   - Это нормально, PyQt5 и зависимости занимают много места
   - Можно попробовать UPX сжатие (уже включено в spec)

3. **Ошибки при запуске:**
   - Проверьте логи в `speech_manager.log`
   - Убедитесь что все зависимости установлены
