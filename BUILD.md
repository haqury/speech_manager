# 🔨 Сборка SpeechManager.exe

## 🚀 Быстрый старт

### 1. Убедитесь что PyInstaller установлен:
```bash
pip install pyinstaller
```

### 2. Закройте SpeechManager.exe если запущен
```
Проверьте:
- Панель задач
- Системный трей (иконка микрофона)
- Диспетчер задач
```

### 3. Запустите сборку:

**Windows:**
```bash
build.bat
```

**Или вручную:**
```bash
pyinstaller --clean build_exe.spec
```

---

## 📦 Результат

**Файл:** `dist\SpeechManager.exe`

**Размер:** ~80-120 MB (зависит от зависимостей в venv)

---

## ✅ Что включено

- ✅ Все Python модули
- ✅ PyQt5 (только нужные модули)
- ✅ SoundDevice
- ✅ SpeechRecognition
- ✅ NumPy
- ✅ Windows API (pywin32)
- ✅ config.json

---

## ❌ Что исключено

Для уменьшения размера исключены:
- ❌ torch, tensorflow (ML библиотеки)
- ❌ pandas, scipy (анализ данных)
- ❌ matplotlib (графики)
- ❌ Jupyter/IPython
- ❌ Лишние PyQt5 модули

---

## 🐛 Решение проблем

### "PermissionError: SpeechManager.exe"
```
Закройте запущенный SpeechManager.exe:
1. Найдите в трее/панели задач
2. Или откройте Диспетчер задач (Ctrl+Shift+Esc)
3. Завершите процесс SpeechManager.exe
```

### "ModuleNotFoundError в .exe"
```
Добавьте модуль в hiddenimports в build_exe.spec:
hiddenimports=[
    'your_module_name',
]
```

### "Слишком большой .exe"
```
Используйте чистый venv:
1. Удалите .venv
2. python -m venv .venv
3. .venv\Scripts\activate
4. pip install -r requirements.txt
5. pip install pyinstaller
6. build.bat
```

---

## 📝 Особенности .exe

- ✅ **Portable** - один файл, не требует установки
- ✅ **No Python** - Python не нужен на целевой системе
- ✅ **Config** - создает config.json автоматически
- ✅ **Logs** - создает speech_manager.log
- ✅ **System tray** - работает иконка в трее
- ✅ **Hotkeys** - горячие клавиши работают

---

## 🚀 Распространение

### Только .exe:
```
Отправьте: dist\SpeechManager.exe
```

### С настройками:
```
Отправьте вместе:
- dist\SpeechManager.exe
- config.json (ваши настройки)
```

При первом запуске .exe автоматически создаст config.json если его нет.

---

**Готово! 🎉**
